version: "3.9"

services:
  tevolvers-api:
    container_name: tevolvers-api
    build:
      context: .
      dockerfile: /Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - tevolvers_data:/data
    restart: unless-stopped

volumes:
  tevolvers_data: